package controller;

/**
 * An enumeration that represents the result of a phase:
 * NEXT phase, the player's VICTORY or the player's DEFEAT, DISCONNECTED when occurs the connection is closed.
 * @author Fumagalli
 */

public enum PhaseResult {VICTORY, NEXT, DEFEAT,DISCONNECTED}
