package controller;

import model.BoardWorker;

public class Action {

    final BoardWorker worker;
    final int[] destination;
    //null: movement action
    final boolean buildDome;

    private Action(BoardWorker worker, int[] destination, boolean getDome){
    }

    public createMove(worker, destination){

    }

    public createBuild(worker, destination, buildDome){}
}
